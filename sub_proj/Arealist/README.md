# 已退役：Arealist SQL 生成器

自 2026-07-17 起，`i_area_list` 以网站管理后台中的数据库记录为唯一来源。
`toAreaList.py` 和 `IAreaList.sql` 工作流已经停用，不得再根据 Markdown 重建或
覆盖地点目录。

`AreaList.md` 只作为迁移前的历史快照保留；其中的 `emit`、行顺序和地点资料均
不会再影响网站。显示开关、排序、资料编辑、新增、归档及 `sheetname` 重命名均在
网站“后台管理 → 地点目录”中完成。
